{
    "dependencies": {
        "express": "^4.17.1",
        "nodemon": "^2.0.7",
        "socket.io": "^4.0.1",
        "http" : "^0.0.1-security"
    },
    "scripts": {
        "dev" : "npm i && nodemon server.js --legacy-watch",
        "start": "node server.js"
    }
}